/**  Las expresiones regulares tambien son objetos . Estos patrones se
 * utilizan en los metodos exec y test de RegExp , así como los métodos match
 * replace,search,split de String .
*/
let patron = new RegExp('ab+c')

// esta expresion ofrece la compilacion en tiempo de compilacion
// utilice esta expresion cuando el patron cambiara