const fn = (texto)=>{

    // representacion literal de la expresion  literal
    let patron =/\w+/
    return patron.test(texto)
}
const fn_1 = (email)=>{
    let patron = //
}
console.log(fn('asdas'));